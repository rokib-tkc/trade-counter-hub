// Optional defaults. For a shared installation, paste the deployed Apps Script URL
// and access key below. Leaving them blank allows each browser to enter them in Settings.
window.APP_CONFIG = {
  API_URL: "",
  API_KEY: "",
  OPEN_HOUR: 8,
  CLOSE_HOUR: 17,
  SLOT_MINUTES: 30,
  WORKING_DAYS: [1, 2, 3, 4, 5]
};
